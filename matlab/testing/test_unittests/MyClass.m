
classdef MyClass
    methods
        function result = add(~, x, y)
            result = x + y;
        end
        
        function result = sub(~, x, y)
            result = x - y;
        end
        
        function result = mul(~, x, y)
            result = x * y;
        end
    end
end

